# 기여 안내

오탈자 제보, 설명이 사실과 어긋난 곳 지적, 예제 코드 개선을 환영한다.
가벼운 것은 [이슈](https://github.com/benelog/flashcard/issues)로, 고칠 내용이 분명하면 풀 리퀘스트로 보내면 된다.

## 기여한 내용의 라이선스

**보낸 기여는 그 기여가 놓이는 자리의 라이선스로 공개된다.**

| 기여한 곳 | 적용되는 라이선스 |
|---|---|
| `book/`의 책 원고 | [CC BY-NC-SA 4.0](https://creativecommons.org/licenses/by-nc-sa/4.0/deed.ko) |
| 그 밖의 모든 코드 | [MIT](./LICENSE) |

**여기에 더해, 저자가 그 기여를 다른 형태로 출판하는 것에도 함께 허락하는 것으로 본다.**
종이책 출간처럼 CC BY-NC-SA 4.0이 금지하는 상업적 이용까지 포함한다(저작권 자체를 넘기는 것은 아니고, 기여자는 자기 기여를 자유롭게 다른 곳에 쓸 수 있다).

이 조건이 필요한 이유는 이렇다.
원고 일부의 저작권이 여러 사람에게 흩어져 있으면, 나중에 종이책을 낼 때 기여자 모두에게 다시 허락을 받아야 한다.
연락이 닿지 않는 사람이 한 명만 있어도 그 문단을 통째로 들어내야 한다.
문장 몇 줄을 고쳐 준 사람에게 몇 년 뒤 연락하는 일을 서로 겪지 않기 위한 조건이다.

동의하지 않는다면 풀 리퀘스트 설명에 그렇게 적어 주면 된다.
그 기여는 웹 공개본에만 반영하고 종이책에서는 뺀다.

## 원고를 고칠 때

- 원고는 AsciiDoc(`.adoc`)이고 집필 규약은 `book/CLAUDE.md`에 있다. 문체('~다'체), 장 참조 토큰(`{ch-파일명}`), 코드 인용(`include::`) 규칙을 따른다.
- **코드를 원고에 베껴 넣지 않는다.** 저장소의 실제 코드를 `include::`로 인용한다.
- 확인: `cd book && npm run build` (인용이 깨졌거나 링크가 끊겼으면 여기서 잡힌다)

## 코드를 고칠 때

Go 모듈이 둘이므로 각 디렉터리에서 따로 확인한다.

```
cd flashcard-advanced && go build ./... && go vet ./... && go test ./...
cd flashcard-basic    && go build ./... && go vet ./... && go test ./...
```

- 코드에 붙은 `// tag::이름[]` … `// end::이름[]` 주석은 책이 그 부분을 인용하는 표식이다. 지우거나 이름을 바꾸면 책 빌드가 깨진다.
- 책이 인용하는 코드를 고쳤으면 `cd book && npm run build`까지 돌린다.

## 브랜치

작업은 `main`에서 갈라 `main`으로 보낸다. `release`는 운영 배포 전용이라 직접 커밋하지 않는다.
